Joe Fair's RPN Example
This is a reverse polish notation calculator.  On the command line, it handles
-Integers and Floats
-Incorrect input
-q to quit
-c to clear the stack
Usage:  ruby [-d] rpn.rb

To run the tests, use ruby test_rpn_calculator.rb

The -d allows you to see the stack before and after a calculation.
